packages:
  - 'apps/*'
  - 'packages/*'
allowBuilds:
  core-js: true
  esbuild: true
  sharp: true
  workerd: true
# First-party packages we publish ourselves — exempt from the minimum-release-age
# supply-chain cooldown (the cooldown guards against compromised third-party
# publishes; it doesn't apply to our own code, and otherwise blocks deploying a
# just-published contrail for ~24h). Scope glob so we don't re-add per version.
minimumReleaseAgeExclude:
  - '@atmo-dev/*'
  - '@atcute/bluesky@4.0.19'
  - '@atcute/lexicons@2.1.0'
patchedDependencies:
  '@atmo-dev/contrail-appview@0.12.2': patches/@atmo-dev__contrail-appview@0.12.2.patch
  '@atmo-dev/contrail-base@0.12.2': patches/@atmo-dev__contrail-base@0.12.2.patch
